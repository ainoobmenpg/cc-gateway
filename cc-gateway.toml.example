# cc-gateway 設定ファイル
#
# このファイルを cc-gateway.toml にリネームして使用してください。
# 設定ファイル内の ${VAR_NAME} は環境変数の値に置換されます。
#
# 設定の優先順位:
# 1. 環境変数
# 2. cc-gateway.toml 設定ファイル
# 3. デフォルト値

# ============================================================================
# LLM 設定
# ============================================================================
[llm]
# API プロバイダー: "claude" または "openai" (または "minimax")
# provider = "claude"      # Anthropic Claude API
# provider = "openai"      # OpenAI 互換 API (GLM, ZAI など)
# provider = "minimax"     # MiniMax API
provider = "openai"

# モデル名
# Claude: "claude-sonnet-4-20250514", "claude-opus-4-6-20251114"
# GLM: "glm-4.7", "glm-4-flash"
# MiniMax: "MiniMax-M2.5", "MiniMax-M2"
model = "glm-4.7"

# API キー（環境変数から取得を推奨）
api_key = "${LLM_API_KEY}"

# カスタム API エンドポイント（オプション）
# GLM: "https://api.z.ai/api/coding/paas/v4"
# MiniMax: "https://api.minimax.io/v1"
base_url = "https://api.z.ai/api/coding/paas/v4"

# ============================================================================
# MiniMax 設定例（コメントアウト）
# ============================================================================
# [llm]
# provider = "minimax"
# model = "MiniMax-M2.5"
# api_key = "${MINIMAX_API_KEY}"
# base_url = "https://api.minimax.io/v1"

# ============================================================================
# Discord 設定（オプション）
# ============================================================================
[discord]
# Discord Bot トークン
token = "${DISCORD_BOT_TOKEN}"

# 管理者ユーザー ID リスト（コマンド実行権限を持つユーザー）
admin_user_ids = [123456789, 987654321]

# ============================================================================
# HTTP API 設定
# ============================================================================
[api]
# HTTP API サーバーのポート
port = 3000

# API 認証キー（オプション、設定した場合のみ認証が必要）
# key = "${API_KEY}"

# ============================================================================
# メモリ設定
# ============================================================================
[memory]
# SQLite データベースファイルのパス
db_path = "data/cc-gateway.db"

# ============================================================================
# MCP 設定
# ============================================================================
[mcp]
# MCP 統合を有効にするかどうか
enabled = true

# MCP 設定ファイルパス
config_path = "mcp.json"

# ============================================================================
# スケジューラー設定
# ============================================================================
[scheduler]
# スケジューラーを有効にするかどうか
enabled = true

# スケジュール設定ファイルパス
config_path = "schedule.toml"
